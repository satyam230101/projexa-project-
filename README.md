
  # Image Card with Hover Reveal

  This is a code bundle for Image Card with Hover Reveal. The original project is available at https://www.figma.com/design/ZITRKm08JKiry5KnAck6jP/Image-Card-with-Hover-Reveal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  